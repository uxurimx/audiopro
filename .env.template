# Copia este archivo a .env y rellena tus claves.
# Las variables aquí tienen MÁXIMA prioridad sobre config.yaml.

# OpenAI — necesario para STT/traducción/TTS en modo cloud
OPENAI_API_KEY=sk-...

# Overrides de config.yaml via env vars (usa __ como separador anidado)
# Ejemplos:
#   AUDIO__INPUT_DEVICE=JBL
#   TRANSLATION__PROVIDER=ollama
#   TTS__PROVIDER=edge_tts
#   ANC__DEFAULT_LEVEL=3
